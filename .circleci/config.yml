version: 2
jobs:
  build: &build
    docker:
      - image: sicz/dockerspec@sha256:b06e045ef6f508591777ed89e6fad6805d9b475ae39453d5ac022d26cda48437
    working_directory: ~/docker-lighttpd
    environment:
      - MAKEFLAGS=--no-print-directory
      - RSPEC_FORMAT=doc
    steps:
      - checkout
      - setup_remote_docker:
          version: 17.06.0-ce
      - run:
          name: Remote Docker engine version and images
          command: |
            docker --version
            docker-compose --version
            docker image ls
      - run:
          name: Checkout make files
          command: |
            cd ..
            curl -sSL https://github.com/SICZ/Mk/archive/master.tar.gz | tar -xzf -
            mv Mk-master Mk
      - run:
          name: sicz/lighttpd:latest
          command: |
            make ci
      - run:
          name: sicz/lighttpd:devel
          working_directory: devel
          command: |
            make ci
      - deploy:
          name: Push images to Docker Hub
          command: |
            if [ "${CIRCLE_BRANCH}" == "master" ]; then
              docker login -u ${DOCKER_HUB_USER} -p ${DOCKER_HUB_PASSWD}
              make docker-push-all
            fi
